\documentclass[tikz,border=10pt]{standalone}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usetikzlibrary{arrows.meta,positioning,fit}
\definecolor{navy}{HTML}{15324B}
\definecolor{blue}{HTML}{2F80C1}
\definecolor{cyan}{HTML}{DCEFF7}
\definecolor{orange}{HTML}{E8873A}
\definecolor{green}{HTML}{3B8F72}
\tikzset{
  box/.style={draw=navy,rounded corners=3pt,very thick,align=center,
    text width=2.85cm,minimum height=1.18cm,inner xsep=4pt,fill=white,
    font=\sffamily\fontsize{9.6}{10.8}\selectfont},
  arr/.style={-{Latex[length=3mm]},very thick,navy},
  note/.style={font=\sffamily\fontsize{9.6}{10.8}\selectfont,align=center,text=navy}
}
\begin{document}
\begin{tikzpicture}[node distance=0.68cm and 0.72cm]
  \node[box,fill=cyan] (pixel)
    {Píxel y\\vecindad local};
  \node[box,right=of pixel] (features)
    {19 variables\\9 color; 3 gradiente\\3 multiescala; 4 locales};
  \node[box,right=of features,fill=orange!13] (sample)
    {Muestreo por lámina\\1 500 de objeto\\1 500 de fondo};
  \node[box,right=of sample,fill=blue!12] (forest)
    {Búsqueda aleatoria\\\texttt{GroupKFold}\\por identidad};
  \draw[arr] (pixel)--(features);
  \draw[arr] (features)--(sample);
  \draw[arr] (sample)--(forest);

  \node[box,below=of forest,fill=green!13] (probability)
    {Probabilidad por píxel\\promedio de\\N árboles};
  \node[box,left=of probability] (threshold)
    {Umbral seleccionado\\sobre láminas\\completas};
  \node[box,left=of threshold,fill=orange!10] (postprocess)
    {Cierre, relleno\\y filtro de\\componentes};
  \node[box,left=of postprocess,fill=cyan] (mask)
    {Máscara binaria\\para evaluación};
  \draw[arr] (forest)--(probability);
  \draw[arr] (probability)--(threshold);
  \draw[arr] (threshold)--(postprocess);
  \draw[arr] (postprocess)--(mask);

  \node[note,draw=blue,dashed,rounded corners=3pt,inner xsep=8pt,inner ysep=4pt,
    text width=13.1cm,below=0.58cm of postprocess.south east]
    {Las coordenadas de la plantilla se excluyen del modelo principal.\\
     Así se evita memorizar su distribución espacial.};
\end{tikzpicture}
\end{document}
